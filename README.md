# AWS S3 & SDK

by Garrett Gunnell

## Installation

To use this program you must install the following libraries:

```
pip install pyqt5
pip install boto3
```

You can then start the program with

```
python main.py
```